#include<bits/stdc++.h>
using namespace std;

bool binarySearch(vector<int> arr,int n,int val){
  int s=0;int e=n;int mid=s+(e-s)/2;
  while(s<=e){
    if(arr[mid]==val){
        return true;
    }
    else if(arr[mid]<val){
        s=mid+1;
    }
    else if(arr[mid]>val){
        e=mid-1;
    }
  }
  return false;
}
int main(){
  vector<int> arr={1,4,7,9,12,19,20};
  int n=arr.size();
  if(binarySearch(arr,n,9)){
      cout<<"Number is present"<<endl;
  }
  else{
      cout<<"Number is not present"<<endl;
  }
  return 0;
}
