class Solution
{
    public int binary_to_decimal(String str)
    {
       int a=1,ans=0;
     for(int i=str.length()-1;i>=0;i--)
     {
       ans+=(str.charAt(i)-'0')*a;
       a*=2;
     }
     return ans;
    }
}
